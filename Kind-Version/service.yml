apiVersion: v1
kind: Service
metadata:
  name: httpd-service
spec:
  type: LoadBalancer # Use NodePort if on a local cluster like Minikube
  selector:
    app: web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80