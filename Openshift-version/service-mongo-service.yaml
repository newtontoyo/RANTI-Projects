apiVersion: v1
kind: Service
metadata:
  name: -service
  labels:
    app: web
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - name: http
      protocol: TCP
      port: 27017         # Service port (inside cluster)
      targetPort: 27017    # Container port
