apiVersion: apps/v1
kind: Deployment
metadata:
  name: vote
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vote
  template:
    metadata:
      labels:
        app: vote
    spec:
      containers:
      - name: vote
        image: image-registry.openshift-image-registry.svc:5000/voting-app/vote:latest
        ports:
        - containerPort: 5000
